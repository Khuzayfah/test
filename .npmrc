# Use the Vercel NPM registry
registry=https://registry.npmjs.org/

# Ensure consistent package installations
save-exact=true

# Improve CI performance
prefer-offline=true
progress=false

# Use non-interactive mode for CI environments
fund=false
audit=false

# Configure package lock behavior
package-lock=true 